import EasterTimer from '../components/EasterTimer';

export default function EasterPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-b from-purple-50 to-blue-50 dark:from-purple-950 dark:to-blue-950">
      <EasterTimer />
    </div>
  );
} 